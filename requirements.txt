# Core numerics and data handling
numpy>=1.26
scipy>=1.11
pandas>=2.1
pyarrow>=14.0
scikit-learn>=1.4
scikit-image>=0.22

# Deep learning (Apple Silicon: MPS backend is used automatically when available)
torch>=2.2
torchvision>=0.17
# transformers is pinned below 5: the 5.x line renames Swin backbone
# parameters, so Mask2Former Cityscapes checkpoints load with randomly
# initialized encoders (verified 2026-08-06 with transformers 5.14).
transformers>=4.46,<5
timm>=1.0
open_clip_torch>=2.24

# Geospatial I/O (MARIDA Sentinel-2 GeoTIFF patches)
rasterio>=1.3

# Plotting and configuration
matplotlib>=3.8
PyYAML>=6.0
tqdm>=4.66

# Development
pytest>=8.0
ruff>=0.4
